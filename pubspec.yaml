name: route_e_commerce_v2
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_native_splash:
  icons_launcher:
  flutter_bloc:
  icons_plus:
  flutter_localizations:
    sdk: flutter
  intl: any
  flutter_svg: ^2.2.0
  google_fonts: ^6.2.1
  dots_indicator: ^4.0.1
  cached_network_image: ^3.4.1
  dio:
  pretty_dio_logger:
  get_it:
  injectable:
  retrofit:
  json_annotation:
  cherry_toast: ^1.13.0
  shared_preferences:
  shimmer: ^3.0.0


dev_dependencies:
  injectable_generator:
  build_runner:
  retrofit_generator:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
    - assets/svgs/
